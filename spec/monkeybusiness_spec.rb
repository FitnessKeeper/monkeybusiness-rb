require 'spec_helper'

describe Monkeybusiness do
  it 'has a version number' do
    expect(Monkeybusiness::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
